# Fixtures de dados

Este diretório contém dados de fixture para os testes Playwright do projeto.

Use `qa-automation/fixtures/data.js` para importar nomes e estruturas de dados consistentes entre os testes.

Exemplos:

```js
import { teamNames, competencyNames, employeeNames, buildCyclePayload } from '../fixtures/data.js';
```
